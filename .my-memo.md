# チェックする
cargo check --target x86_64-linux-android                           
cargo check

# crate io にログインする
cargo login [key]
cargo publish         

# ts から js などをビルドする
cd api
npx rollup -c            

# npm に公開/更新する
cd api
npm adduser
npm publish --access public