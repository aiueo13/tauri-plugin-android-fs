# チェックする
chmod +x .check_all_features.sh
./.check_all_features.sh

featureを増やしたら.check_all_features.shにあるFEATURESに付け加える。

# crate io にログインする
cargo login [key]   
cargo publish         

# ts から js などをビルドする
cd api   
npx rollup -c            

# npm に公開/更新する
cd api   
npm adduser   
npm publish --access public   